# Choppy-audio fix (2026-07-21).
# The daemon/app reach PipeWire through the ALSA pulse plugin ->
# pipewire-pulse. Do not let those streams request < 21 ms buffers.
pulse.properties = {
    pulse.min.req     = 1024/48000
    pulse.min.quantum = 1024/48000
    pulse.min.frag    = 1024/48000
}
